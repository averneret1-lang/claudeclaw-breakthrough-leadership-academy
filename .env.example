# Anthropic auth is handled via OAuth (claude auth login).
# Only set this if you prefer a raw API key instead.
# ANTHROPIC_API_KEY=

# Node environment
NODE_ENV=production

# Log level (debug | info | warn | error)
LOG_LEVEL=info
